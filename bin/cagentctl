#!/usr/bin/env bash
# cagent operator CLI. Uses the repo venv python so deps (jsonschema) are available.
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PY="$ROOT/.venv/bin/python"
[ -x "$PY" ] || PY="python3"
exec "$PY" -m cagent.cli "$@"
